# COMMANDS

Updated: 2026-05-15

## Core Commands

```powershell
npm run build
```

## Reference Trigger

```powershell
powershell -File tools/reference-benchmark.ps1
```

## Notes

- `pnpm build` is the project-intended command when `pnpm` is installed locally or available through corepack.
- Add new verified commands here when they become part of the stable workflow.
- Do not invent commands that are not present in the repo unless they are explicitly added and verified.
