# Meridian Clean Tasks

Updated: 2026-05-14

## Active

- Keep the OCR canonical path as `tools/ocr_source_text.mjs`.
- Keep OCR output cached locally and stored in the database.
- Keep document grouping and split/merge decisions in the document protocol layer.
- Keep client boundaries strict and separate.
- Keep `client_001` historical and `Hub Interior Group Corporation` active.

## Next

- Use the new protocol on incoming scans and documents.
- Update the protocol summaries and any operator-facing notes from the new rules.
- If a future change is needed, update control files first, then code.

## Ongoing

- Update `STATE.md`, `HANDOFF.md`, `TASKS.md`, `RULES.md`, and `DECISIONS.md` when the project model changes.
- Keep the terminology consistent:
  - raw scan
  - document
  - receipt
  - attachment
  - archive PDF
  - spreadsheet row

